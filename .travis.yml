language: python

python:
    - '2.7'
    - '3.3'
    - '3.4'
    - '3.5'

# whitelist
branches:
    only:
        - master

addons:
  apt:
    packages:
    - seq-gen

script:
    py.test --cov=pandascharm.py --pep8

after_success:
  - codecov
